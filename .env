# Direct API calls (requires backend CORS for http://localhost:5173)
VITE_API_BASE_URL=https://localhost:7007/api

# Vite dev proxy target (use VITE_API_BASE_URL=/api to route through proxy instead)
VITE_API_PROXY_TARGET=https://localhost:7007
